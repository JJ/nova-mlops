use Test::Text; # -*- mode: cperl -*-

just_check( '.','.', 'English',0 );       # 0 == not done
just_check( 'text','.', 'English', 0 ); # 0 == still not done
just_check( 'sessions','.', 'English' );
